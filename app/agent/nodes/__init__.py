"""Pure decision logic for the agent's nodes: no model, no database, no graph.

The Agent in graph.py wires these into LangGraph; keeping the decisions here
makes each one testable on its own with plain inputs.
"""
