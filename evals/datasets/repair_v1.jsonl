{"id": "r01", "question": "What is the total revenue?", "broken_sql": "SELECT SUM(revenue) FROM orders", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: no such column: revenue", "reference_sql": "SELECT SUM(i.unit_price * i.quantity) FROM order_items i JOIN orders o ON o.id = i.order_id WHERE o.status != 'cancelled'", "order_matters": false}
{"id": "r02", "question": "List the customers in Pune.", "broken_sql": "SELECT name FROM buyers WHERE city = 'Pune'", "error_code": "UNKNOWN_TABLE", "error_detail": "The table 'buyers' does not exist. Available tables: customers, order_items, orders, products.", "reference_sql": "SELECT name FROM customers WHERE city = 'Pune'", "order_matters": false}
{"id": "r03", "question": "How many orders are there per status?", "broken_sql": "SELECT status, COUNT(*) FROM orders GROUP status", "error_code": "PARSE_ERROR", "error_detail": "The SQL could not be parsed: Invalid expression / Unexpected token (line 1, column 48)", "reference_sql": "SELECT status, COUNT(*) FROM orders GROUP BY status", "order_matters": false}
{"id": "r04", "question": "Show each order ID with the customer's name.", "broken_sql": "SELECT id, name FROM orders JOIN customers ON customers.id = orders.customer_id", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: ambiguous column name: id", "reference_sql": "SELECT o.id, c.name FROM orders o JOIN customers c ON c.id = o.customer_id", "order_matters": false}
{"id": "r05", "question": "How many orders were placed in 2024?", "broken_sql": "SELECT COUNT(*) FROM orders WHERE YEAR(order_date) = 2024", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: no such function: YEAR", "reference_sql": "SELECT COUNT(*) FROM orders WHERE strftime('%Y', order_date) = '2024'", "order_matters": false}
{"id": "r06", "question": "How many orders were placed each month?", "broken_sql": "SELECT DATE_TRUNC('month', order_date) AS m, COUNT(*) FROM orders GROUP BY m", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: no such function: DATE_TRUNC", "reference_sql": "SELECT strftime('%Y-%m', order_date) AS m, COUNT(*) FROM orders GROUP BY m", "order_matters": false}
{"id": "r07", "question": "What are the 3 most expensive products?", "broken_sql": "SELECT name FROM products ORDER BY price DESC LIMIT -3", "error_code": "INVALID_LIMIT", "error_detail": "LIMIT must be a non-negative whole number, for example LIMIT 10.", "reference_sql": "SELECT name FROM products ORDER BY price DESC LIMIT 3", "order_matters": false}
{"id": "r08", "question": "What is the revenue for each category?", "broken_sql": "SELECT p.category, SUM(i.unit_price * i.qty) FROM order_items i JOIN products p ON p.id = i.product_id GROUP BY p.category", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: no such column: i.qty", "reference_sql": "SELECT p.category, SUM(i.unit_price * i.quantity) FROM order_items i JOIN orders o ON o.id = i.order_id JOIN products p ON p.id = i.product_id WHERE o.status != 'cancelled' GROUP BY p.category", "order_matters": false}
{"id": "r09", "question": "Which customers signed up in 2024?", "broken_sql": "SELECT name FROM customers WHERE signup_date LIKE '2024%' AND", "error_code": "PARSE_ERROR", "error_detail": "The SQL could not be parsed: Required keyword: 'expression' missing for <class 'sqlglot.expressions.core.And'> (line 1, column 61)", "reference_sql": "SELECT name FROM customers WHERE signup_date LIKE '2024%'", "order_matters": false}
{"id": "r10", "question": "What is the average order value?", "broken_sql": "SELECT AVG(total) FROM orders", "error_code": "EXECUTION_ERROR", "error_detail": "The database rejected the query: no such column: total", "reference_sql": "SELECT AVG(t) FROM (SELECT SUM(i.unit_price * i.quantity) AS t FROM order_items i JOIN orders o ON o.id = i.order_id WHERE o.status != 'cancelled' GROUP BY o.id)", "order_matters": false}
