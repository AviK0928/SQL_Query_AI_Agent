# Render's build command is `pip install -r requirements.txt`. This file only
# delegates to pyproject.toml, which is the single source of dependencies, so
# the build command did not have to change (D-entry in Phase 1).
# Editable, so app/ keeps resolving database/ and frontend/ from the repo root.
-e .
